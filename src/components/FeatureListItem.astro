---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<li class={`flex flex-row gap-4 items-start ${className} `}>
  <img src='/images/icon-list.svg' alt='List icon' />
  <slot />
</li>
